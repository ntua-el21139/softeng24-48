import React from "react";
import { cn } from "../../lib/utils";
import { cva } from "class-variance-authority";
const inputVariants = cva("w-full flex items-center justify-center sm:px-[1.25rem] cursor-text text-white-a700 text-[1.25rem] font-semibold rounded-[32px]", {
  // ... rest of the code ...
}); 